# ============================
# LunaTV 环境变量配置
# ============================
# 复制此文件为 .env.local 并填入实际值

# ---- 必填 ----

# 站长账号
USERNAME=admin

# 站长密码（以 bcrypt 哈希存储）
PASSWORD=your_strong_password

# 存储类型：upstash | localstorage | memory
#   upstash      = Upstash Redis（生产环境）
#   localstorage = 客户端 localStorage（无服务器端持久化）
#   memory       = 纯内存存储（开发测试用，重启后应用数据消失；登录 cookie 仍按过期时间保留）
NEXT_PUBLIC_STORAGE_TYPE=upstash

# Upstash Redis 配置（STORAGE_TYPE=upstash 时必填）
UPSTASH_URL=https://your-instance.upstash.io
UPSTASH_TOKEN=AXxxxxxxxxxxxx

# ---- 可选：站点配置 ----

# 站点名称（显示在浏览器标签和页面中）
# NEXT_PUBLIC_SITE_NAME=LunaTV

# 站点公告
# ANNOUNCEMENT=欢迎使用 LunaTV

# 站点 URL（用于 m3u8 URL 重写）
# SITE_BASE=https://your-domain.com

# ---- 可选：安全 ----

# Cron 任务认证密钥（Vercel 自动通过 Authorization 头发送）
# CRON_SECRET=your_cron_secret

# ---- 可选：搜索 ----

# 搜索最大页数（1-50）
# NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 是否开启流式搜索输出
# NEXT_PUBLIC_FLUID_SEARCH=true

# ---- 可选：内容过滤 ----

# 关闭色情内容过滤（默认开启）
# NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false

# ---- 可选：豆瓣数据代理 ----

# 豆瓣数据代理方式：direct | cors-proxy-zwei | cmliussss-cdn-tencent | cmliussss-cdn-ali | custom
# NEXT_PUBLIC_DOUBAN_PROXY_TYPE=direct

# 自定义豆瓣数据代理 URL（DOUBAN_PROXY_TYPE=custom 时使用）
# NEXT_PUBLIC_DOUBAN_PROXY=

# ---- 可选：豆瓣图片代理 ----

# 豆瓣图片代理类型：direct | server | img3 | cmliussss-cdn-tencent | cmliussss-cdn-ali | custom
# NEXT_PUBLIC_DOUBAN_IMAGE_PROXY_TYPE=direct

# 自定义豆瓣图片代理 URL（DOUBAN_IMAGE_PROXY_TYPE=custom 时使用）
# NEXT_PUBLIC_DOUBAN_IMAGE_PROXY=
